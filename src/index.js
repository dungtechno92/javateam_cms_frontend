import React, { Component } from 'react';
import ReactDOM from 'react-dom';
import RouterLink from '../src/container/router/RouterLink.jsx'

ReactDOM.render(
    <RouterLink />,
    document.getElementById('root')
);
